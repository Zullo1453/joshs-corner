(() => { document.querySelectorAll('.currency-chart').forEach((canvas) => { let points; try { points = JSON.parse(canvas.dataset.points || '[]'); } catch (_) { return; } if (points.length < 2) return; const rect = canvas.getBoundingClientRect(), scale = window.devicePixelRatio || 1, width = Math.max(260, Math.round(rect.width)), height = 150; canvas.width = width * scale; canvas.height = height * scale; const ctx = canvas.getContext('2d'); ctx.scale(scale, scale); const values = points.map(point => Number(point.rate)), min = Math.min(...values), max = Math.max(...values), span = max - min || 1, pad = 16; ctx.strokeStyle = '#314746'; ctx.lineWidth = 1; ctx.beginPath(); ctx.moveTo(pad, height - pad); ctx.lineTo(width - pad, height - pad); ctx.stroke(); ctx.strokeStyle = '#86bbb4'; ctx.lineWidth = 2; ctx.beginPath(); values.forEach((value, index) => { const x = pad + index * (width - pad * 2) / (values.length - 1), y = pad + (max - value) * (height - pad * 2) / span; index ? ctx.lineTo(x, y) : ctx.moveTo(x, y); }); ctx.stroke(); }); })();
